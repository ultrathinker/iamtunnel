// Package design carries the Telescope design language ported from
// umtunnel's Design.cs: the type scale, the Light/Dark palettes, and the
// Poster, Deck, Facts, Card, Rule, Dot, Pulse and Tabs primitives (SPEC
// §7.1). It is presentation only — no role logic, no callbacks into the
// application, no business types from state or ACL.
package design
